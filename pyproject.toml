[tool.poetry]
name = "deep-scholar"
version = "0.1.0"
description = "CS495 Deep Scholar — CRR Binomial + Kelly Criterion AMD Options Pipeline"
authors = []
readme = "README.md"
license = "MIT"
package-mode = false

[tool.poetry.dependencies]
python     = "^3.13"
numpy      = "^2.0"
pandas     = "^2.2"
matplotlib = "^3.9"
yfinance   = "^0.2"
scipy      = "^1.13"
pyyaml     = "^6.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
